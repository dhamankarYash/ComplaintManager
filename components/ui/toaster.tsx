"use client"

export function Toaster() {
  // The ToastProvider already handles rendering toasts
  // This component exists for compatibility but doesn't need to render anything
  // since the ToastProvider renders the toasts directly
  return null
}
